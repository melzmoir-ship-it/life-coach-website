# Life Coach Website (Static Demo)

Static HTML site — no Node, no backend, no env vars required for local preview.

## Run locally

```bash
cd life-coach-website
python -m http.server 8000
```

Open: http://localhost:8000
